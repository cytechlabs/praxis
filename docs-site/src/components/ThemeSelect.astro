---
// Intentionally renders nothing. The documentation is dark-only to match
// praxisfleet.com, so there is nothing for a theme picker to switch between.
// The theme is pinned in ThemeProvider.astro.
---
