/**
 * The canonical product version, inlined by `astro.config.mjs` from
 * `frontend-next/src/config/version.ts`. Declared here so pages can use it
 * under strict type checking; `src/version.mjs` is what reads it from disk.
 */
declare const __PRAXIS_DOCS_VERSION__: string;
