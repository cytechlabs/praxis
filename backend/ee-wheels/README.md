# Placeholder so the paid-capable build (PRA-134) always has a local wheel
# directory in the build context. Drop the private praxis-ee wheel here for a
# --build-arg PRAXIS_EE=1 build; the wheel itself is gitignored and never
# committed. See docs/editions.md (Building a paid-capable image).
