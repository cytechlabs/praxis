"""Command-line entrypoints for backend operator tools (PRA-160 slice #5).

Subpackages here are invokable as ``python -m app.cli.<module>`` from
inside the backend container, or via the ``praxis-airgap`` console
script (defined in setup.cfg / pyproject when one is added).
"""
